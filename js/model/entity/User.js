export default class User {
  constructor(name, email, password, birthDate, gender) {
    this.name = name;
    this.email = email;
    this.password = password;
    this.birthDate = birthDate;
    this.gender = gender;
  }
}
